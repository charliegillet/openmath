[project]
name = "busy-beaver-6-certificates"
version = "0.1.0"
description = "Evaluation environment for busy-beaver-6-certificates"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]
