[project]
name = "clique-cluster-ramsey-multiplicity"
version = "0.1.0"
description = "Evaluation environment for clique-cluster-ramsey-multiplicity"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]
