[project]
name = "millennium-p-vs-np"
version = "0.1.0"
description = "Evaluation environment for millennium-p-vs-np"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]
