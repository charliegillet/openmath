[project]
name = "diffusion-parabola"
version = "0.1.0"
description = "Evaluation environment for diffusion-parabola"
requires-python = ">=3.11"
dependencies = ["numpy>=1.24"]

[dependency-groups]
dev = ["pytest>=8.0"]
