[project]
name = "circle-packing"
version = "0.1.0"
description = "Evaluation environment for the circle-packing hill"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]
