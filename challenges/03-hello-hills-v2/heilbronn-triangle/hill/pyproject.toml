[project]
name = "heilbronn-triangle"
version = "0.1.0"
description = "Evaluation environment for heilbronn-triangle"
requires-python = ">=3.11"
dependencies = ["mpmath>=1.3"]

[dependency-groups]
dev = ["pytest>=8.0"]
