[project]
name = "kernel-opt"
version = "0.1.0"
description = "Evaluation environment for kernel-opt"
requires-python = ">=3.11"
dependencies = ["numpy>=1.26"]

[dependency-groups]
dev = ["pytest>=8.0"]
